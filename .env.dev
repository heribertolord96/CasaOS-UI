VUE_APP_DEV_IP=localhost
VUE_APP_DEV_PORT=4080
# Optional: set to 1 to call the API from the browser (DevTools shows :4080). Requires CORS on the gateway; default uses dev-server proxy.
# VUE_APP_DEV_DIRECT_API=1
